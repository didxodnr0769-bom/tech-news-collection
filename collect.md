# 매일 뉴스 수집 워크플로우

이 문서는 **매일 아침 5시에 실행되는 뉴스 수집 작업 지침**이다.
macOS `launchd`가 헤드리스 Claude Code를 실행하고, Claude가 이 파일을 읽어 아래 절차를 수행한다.

## 작업 개요

AI · 개발 · 기타 3개 카테고리의 최신 뉴스를 웹 검색으로 수집해
`data/<오늘 날짜>.json` 파일로 저장하고, `data/index.json`을 갱신한다.
**git 커밋·푸시는 호출 쉘 스크립트(`scripts/run-collect.sh`)가 자동으로 처리한다** — Claude는 파일 저장까지만 하고 종료한다.

> ## ⚠️ 필수 원칙: 한국에서 발행된 뉴스·글만 수집한다
> - 출처(`source`)는 **반드시 한국 매체이거나 한국어로 발행된 글**이어야 한다.
> - 영어권 매체(TechCrunch, The Verge, Tom's Hardware 등)의 영문 기사는 **수집하지 않는다.**
>   (해외 소식이라도 한국 매체가 한국어로 보도한 기사라면 수집 가능하다.)
> - 검색은 **한국어 키워드**로 한다.

## 절차

### 1. 오늘 날짜 확인
- `YYYY-MM-DD` 형식 (Asia/Seoul 기준). 예: `2026-05-21`

### 2. 카테고리별 웹 검색 (한국어로 검색)
각 카테고리에 대해 **최근 3~7일 이내**의 뉴스를 한국어 키워드로 검색한다.
(웹 검색으로 정확히 24시간을 끊는 것은 비현실적이므로 최근 며칠 범위로 본다.)

| 카테고리 | 검색 범위 |
|----------|-----------|
| `AI` | 모델 출시, AI 연구, AI 제품·서비스, AI 코딩 도구 |
| `개발` | 프로그래밍 언어, 프레임워크·라이브러리 릴리스, 개발 도구, 오픈소스, 엔지니어링 관행 |
| `기타` | 위 두 카테고리에 속하지 않는 개발자 참고용 소식. 아래 서브카테고리별로 수집한다 |

**`기타` 카테고리의 서브카테고리** — `기타` 기사는 아래 중 하나의 `subcategory` 값을 가진다.

| 서브카테고리 | 수집 범위 |
|--------------|-----------|
| `전자기기` | CPU·GPU·칩, 메모리·반도체, 개발자용 디바이스(노트북·주변기기), 컴퓨팅 인프라 등 하드웨어 소식 |
| `경제` | IT·기술 업계의 경제 소식 — 투자·펀딩, 기업 실적, 시장 동향, 산업 정책 등 |

> 서브카테고리가 추가·변경되면 이 표와 `index.json`의 `subcategories` 값을 함께 갱신한다.

**권장 한국 매체 예시**
- 종합 IT: ZDNet Korea, 전자신문, 디지털데일리, 바이라인네트워크, 블로터
- AI 전문: AI타임스, 인공지능신문
- 개발/보안: GeekNews(news.hada.io), 요즘IT, 데일리시큐
- 경제/산업: 머니투데이, 한국경제, 글로벌이코노믹 등

검색 키워드 예시: `"구글 I/O 2026 제미나이"`, `"npm 공급망 공격"`, `"HBM 메모리 뉴스 2026년 5월"`

### 3. 기사 선별 — 카테고리당 5~10개
- **개발자가 참고할 가치가 있는 것** 우선 (실무 영향, 새 릴리스, 의미 있는 동향)
- 단순 홍보성·중복 기사는 제외
- 한 기사가 여러 카테고리에 걸치면 **가장 주된 성격 하나**로만 태그
- `기타`는 서브카테고리(전자기기·경제)별로 새 소식 양이 다르다 — 무리하게 채우지 말 것 (억지로 늘리지 않는다)

### 4. 기사별 정보 정리
각 기사를 아래 필드로 정리한다:

| 필드 | 설명 |
|------|------|
| `id` | `<날짜>-<카테고리약어>-<번호>` 예: `2026-05-21-ai-01` (약어: ai / dev / etc) |
| `category` | `"AI"` · `"개발"` · `"기타"` 중 하나 (정확히 일치) |
| `subcategory` | `기타` 카테고리 기사에만 넣는다. 값은 `"전자기기"` 또는 `"경제"`. AI·개발 기사에는 넣지 않는다 |
| `tags` | 기사의 핵심 토픽 키워드 **1~2개** 문자열 배열. 모든 기사에 넣는다. 예: `["gemini", "google"]` |
| `title` | 한글 기사 제목 (한국 매체의 원래 제목을 그대로 사용) |
| `summary` | **2~3문장** 한글 요약 |
| `url` | 원문 기사 URL (한국 매체) |
| `source` | 한국 매체명 (예: `"ZDNet Korea"`, `"전자신문"`, `"바이라인네트워크"`) |
| `thumbnail` | 원문 페이지의 `og:image` URL. 없으면 빈 문자열 `""` |
| `published` | 기사 발행일 `YYYY-MM-DD` |

**`tags` 작성 요령**
- 기사를 한눈에 식별하는 **핵심 키워드 1~2개**를 고른다 — 제품명·기업명·기술명·이벤트명 등. (예: 구글이 제미나이 신규 모델을 출시 → `["gemini", "google"]`)
- 영문 제품·기업명은 **소문자**로(`gemini`, `npm`, `cursor`), 한국어 개념은 한국어로(`반도체`, `공급망공격`) 적되 **띄어쓰기 없이** 짧게.
- 여러 기사에서 같은 토픽이면 **표기를 통일**한다 — 값이 정확히 같아야 집계·필터로 묶인다. (`gemini`와 `Gemini`를 섞지 않는다)
- `category`/`subcategory`(AI·개발·기타)와 달리 `tags`는 자유로운 세부 토픽이다. 사이트는 이 태그를 날짜별로 집계해 많은 순으로 보여주고, 태그를 누르면 해당 기사만 필터링한다.

### 5. `data/<날짜>.json` 생성
```json
{
  "date": "2026-05-21",
  "generated_at": "2026-05-21T05:00:00+09:00",
  "articles": [ { ...기사 객체... } ]
}
```
- `articles`는 AI → 개발 → 기타 순서로 정렬
- 이미 같은 날짜 파일이 있으면 덮어쓴다

### 6. `data/index.json` 갱신
- `dates` 배열 맨 앞에 오늘 날짜 추가 (이미 있으면 중복 추가 금지, 최신순 유지)
- `last_updated`를 현재 시각 ISO 8601(+09:00)로 갱신
- `site_title` · `description` · `categories`는 그대로 둔다

### 7. 마무리 — Claude는 파일 저장까지만, git 작업은 쉘이 한다
- 5·6단계의 파일 저장까지만 하고 **종료한다.**
- **Claude는 `git add`·`git commit`·`git push`를 직접 실행하지 않는다.** (이중 커밋 방지)
- 호출한 `scripts/run-collect.sh`가 수집 성공을 확인한 뒤 자동으로 `git add data/ → commit → push`를 수행하며, push되면 GitHub Pages가 사이트를 자동 갱신한다.

## 주의사항
- **출처는 무조건 한국 매체/한국어 글.** 영어권 매체 영문 기사는 제외한다.
- 요약은 사실에 근거해 작성하고, 검색 결과에 없는 내용을 지어내지 않는다.
- `url`은 반드시 실제 접근 가능한 원문 링크여야 한다.
- `published`(발행일)는 확인 가능한 경우에만 적고, 추측해서 지어내지 않는다.
- 검색 결과가 빈약한 날은 기사 수가 적어도 괜찮다 — 품질을 수량보다 우선한다.
