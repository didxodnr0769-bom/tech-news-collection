# 릴리즈 노트

## 2026-05-26

### UI 개선

- **태그 영역 2줄 + 더보기 토글**: 태그 칩이 늘어나도 화면을 과하게 차지하지
  않도록 기본 2줄까지만 표출한다. 칩이 2줄을 넘기면 "더보기" 토글이 나타나며,
  누르면 전체 태그가 펼쳐지고 라벨이 "접기"로 바뀐다. 토글은 알약 모양의 태그
  칩과 명확히 구분되도록 셰브론(▾) 아이콘과 텍스트 링크 스타일로 표시된다.
- **날짜 캘린더 팝업**: 기존 날짜 드롭다운 셀렉트를 커스텀 캘린더 팝업으로
  교체했다. 데이터가 수집된 날짜만 활성화(파란색 강조)되어 클릭할 수 있고,
  년·월 셀렉트로 빠르게 이동할 수 있다. 데이터 범위를 벗어난 년/월은 자동으로
  숨겨지며, 미래 날짜는 자연스럽게 비활성화된다.
- **이전/다음 데이터 날짜 빠른 이동**: 캘린더를 열지 않고도 날짜 버튼 양옆의
  ‹ › 버튼으로 인접한 데이터 날짜로 바로 이동할 수 있다. 양 끝 날짜에서는
  비활성화된다.
- **전체 기간 토글 분리**: 기존에 셀렉트 옵션이었던 "전체 기간"을 독립된 토글
  버튼으로 분리해 한 번에 켜고 끌 수 있게 했다.

### 내부 변경

- `assets/app.js` 의 날짜 컨트롤 로직을 재구성: `state.cal` (캘린더 팝업 상태),
  `state.dateSet` (데이터가 있는 날짜 Set), `state.tagsExpanded` 추가.
- 캘린더 헤더의 셀렉트가 팝업 영역을 벗어나지 않도록 `.cal-head-selects`
  래퍼로 묶어 남는 공간을 균등 분배.
